:local(.Carousel)
  display block
  width 100%
  height 100%
  .carousel__image
    width 100%
    height 100%
    display none
    animation fadeOut 1.5s ease-in-out
    
    &.active
      display flex
    
@keyframes fadeOut 
  from 
    opacity 0
  to 
    opacity 1
